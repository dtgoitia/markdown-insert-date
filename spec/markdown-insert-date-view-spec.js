'use babel';

import MarkdownInsertDateView from '../lib/markdown-insert-date-view';

describe('MarkdownInsertDateView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
